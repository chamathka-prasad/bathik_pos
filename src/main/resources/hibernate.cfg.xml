<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/bathik_pos?createDatabaseIfNotExist=true&amp;useSSL=false&amp;serverTimezone=UTC</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">root</property>

        <!-- JDBC connection pool settings (using HikariCP via Hibernate) -->
        <property name="hibernate.connection.provider_class">org.hibernate.hikaricp.internal.HikariCPConnectionProvider</property>
        <property name="hibernate.hikari.minimumIdle">5</property>
        <property name="hibernate.hikari.maximumPoolSize">20</property>
        <property name="hibernate.hikari.idleTimeout">300000</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Echo all executed SQL to console (set to false in production) -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Automatically create/update database schema -->
        <!-- Options: validate | update | create | create-drop -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Current session context -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Disable contextual LOB creation (for MySQL) -->
        <property name="hibernate.jdbc.lob.non_contextual_creation">true</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="hibernate.enable_lazy_load_no_trans">true</property>

        <!-- Mapped Entity Classes -->
        <mapping class="com.chamathka.bathikpos.entity.User"/>
        <mapping class="com.chamathka.bathikpos.entity.Customer"/>
        <mapping class="com.chamathka.bathikpos.entity.Supplier"/>
        <mapping class="com.chamathka.bathikpos.entity.Product"/>
        <mapping class="com.chamathka.bathikpos.entity.ProductVariant"/>
        <mapping class="com.chamathka.bathikpos.entity.GRN"/>
        <mapping class="com.chamathka.bathikpos.entity.GRNItem"/>
        <mapping class="com.chamathka.bathikpos.entity.Sale"/>
        <mapping class="com.chamathka.bathikpos.entity.SaleItem"/>
    </session-factory>
</hibernate-configuration>
